/**
 * PM2 Ecosystem Configuration
 * App: soar-client (Nuxt 3)
 * Mode: Production build (`pnpm build`)
 */

module.exports = {
  apps: [
    {
      name: 'soar-client',
      cwd: '/var/www/html/soar-client',

      // Nuxt 3 production server entry produced by `pnpm build`
      script: '.output/server/index.mjs',

      // Use Node to run the ESM entry reliably
      interpreter: 'node',

      exec_mode: 'fork',
      instances: 1,

      autorestart: true,
      watch: false,
      max_memory_restart: '512M',

      env: {
        NODE_ENV: 'production',
        HOST: '0.0.0.0',
        NUXT_PORT: 3000,
      },

      // Logs inside the project to avoid /var/log permission issues
      error_file: '/var/www/html/soar-client/storage/logs/pm2/soar-client-error.log',
      out_file: '/var/www/html/soar-client/storage/logs/pm2/soar-client-out.log',
      merge_logs: true,
      log_date_format: 'YYYY-MM-DD HH:mm:ss',
    },
  ],
}
